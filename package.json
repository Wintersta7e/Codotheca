{
  "name": "codotheca",
  "version": "0.0.0",
  "main": "app/out/main/index.js",
  "author": "Codotheca contributors",
  "private": true,
  "description": "A library for the code you have written",
  "license": "GPL-3.0-or-later",
  "workspaces": [
    "app",
    "protocol"
  ],
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "gen": "node protocol/generate.mjs",
    "build:core": "cargo build --release --manifest-path core/Cargo.toml",
    "build:app": "npm run build --workspace app",
    "build": "npm run gen && npm run build:core && npm run build:app",
    "dev": "npm run gen && npm run dev --workspace app",
    "lint": "npm run lint --workspace app && cargo clippy --manifest-path core/Cargo.toml --all-targets -- -D warnings",
    "typecheck": "npm run typecheck --workspace app",
    "check:bundle": "node scripts/check-bundle.mjs",
    "prepare:win": "node scripts/win-prepare.mjs",
    "test": "cargo test --manifest-path core/Cargo.toml && npm run test --workspace protocol && npm run test --workspace app",
    "fmt:check": "cargo fmt --manifest-path core/Cargo.toml -- --check && prettier --check \"app/**/*.{ts,tsx}\" \"scripts/**/*.mjs\" \"protocol/**/*.mjs\""
  },
  "devDependencies": {
    "electron-builder": "^26.15.3",
    "prettier": "^3.3.0"
  },
  "allowScripts": {
    "esbuild@0.25.12": true,
    "esbuild@0.28.2": true,
    "electron-winstaller@5.4.0": true
  }
}
